Texture2D squareTexture : register(t0);
SamplerState squareSampler : register(s0);

struct VSOutput
{
	float4 pos : SV_Position;
	float2 uv : TEXCOORD;
};

float4 ps(VSOutput pixel) : SV_Target0
{
	return float4(squareTexture.Sample(squareSampler, pixel.uv).xyz, 1.0);
}